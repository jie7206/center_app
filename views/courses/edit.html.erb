<h1>編輯課程</h1>
<%= render :partial => 'form', :locals => { :submit_title => '儲存修改' } %>
<p class="del_link"><%= link_to '刪除此課程', @course, :confirm => "你確定要刪除#{@course.title}?", :method => :delete %></p>