<% form_for(@question) do |f| %>
  <%= f.error_messages %>
<% if session[:login_role] == 'admin' %>
  <p>發起人：<%= f.select :member_id, member_ids %></p>
<% else %>  
  <%= f.hidden_field :member_id, :value => session[:login_mid] %>
<% end %>
  <p>對象：<%= f.text_field :to_name %> <%= member_list('question_to_name') %></p>
  <p>分類：<%= f.select :catalog, question_catalogs %></p>
  <p>標題：<%= f.text_field :title %></p>
  <p> <%= f.check_box :is_secret %> 這是給亞伯的悄悄話，別人看不到 ^o^</p>
  <p>說明：<br/><%= f.text_area :description, :size => "50x6" %></p>
  <p>解決方法建議或成功解決的方式：<br/><%= f.text_area :solution, :size => "50x6" %></p>
  <p> <%= f.check_box :is_solved %> 該問題已經獲得解決</p>
  <p><%= f.submit submit_title %>&nbsp;&nbsp;<%= button_to_function '返回前頁', "history.back();" %></p>
<% end %>
